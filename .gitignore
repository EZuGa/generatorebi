# build output
dist/
.output/

# dependencies
node_modules/

# astro
.astro/

# environment variables
# NOTE: .env.production is intentionally committed — it holds only non-secret
# build-time values (public Supabase URL + anon key, site URL). Secrets stay in
# .env locally and in the Cloudflare dashboard.
.env
.env.development

# wrangler / cloudflare
.wrangler/
.dev.vars

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# os / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
